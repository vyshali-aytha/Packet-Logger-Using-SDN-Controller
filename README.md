# Packet-Logger-Using-SDN-Controller
SDN Packet Logger using POX and Mininet. The controller handles PacketIn events and logs packet details while applying basic OpenFlow match–action rules. Network behavior is demonstrated using ping and iperf traffic, validated through Wireshark captures and switch flow table entries.
